package com.crysapp.student.utill;
import android.os.Bundle;
import androidx.recyclerview.selection.Selection;
import androidx.recyclerview.selection.StorageStrategy;
import com.crysapp.student.models.Class;
import java.util.Set;

public class strategy{} /*extends StorageStrategy<Class>{
    public Bundle asBundle(Selection<Class> s){
        Bundle b = new Bundle();
       // b.putParcelableArray(new Class[]);
        //s.forEach(->)
        return b;
    }
    public Selection<Class> asSelection(Bundle b){
        Set s;
        return new Selection(s);
    }
    strategy(){
        super(Class.class);
    }
}*/